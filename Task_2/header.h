#pragma once
typedef long long ll;
ll IncorrectInput();

std::vector<int> sum(std::vector<int>, std::vector<int> );

std::vector<int> convert(ll );

std::vector <int> translate(std::vector <int> );

std::vector<int> numberToVector();